#include <iostream>
#include"vector2.h"

int main() {

    Vector2<double> v = {1.0,2.0};
    Vector2<double> u = {1.0,2.0};
/* 
    for( int i = 0; i < 1; i++)
    {
        v +=v;
    }
    printvector(v);

for( int i = 0; i < 2; i++)
    {
        v -=u;
    }
    printvector(v);

for( int i = 0; i < 1; i++)
    {
        v *= 2.0;
    }
    printvector(v);

for( int i = 0; i < 1; i++)
    {
        v /=2.0;
    }
    printvector(v);

Vector2<double> a = v+u;
printvector(a);

a= v-u;
printvector(a);


a = 2.0*v;
printvector(a);

a = v*2.0;
printvector(a);

auto b = length(v);
std::cout<<b<<std::endl;

b=sqlength(v);
std::cout<<b<<std::endl;

Vector2<double> c=normalize(v);
printvector(c);


double c =dot(v,u);
std::cout<<c<<std::endl;
*/
}